<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet & Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- NFC -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />


    <!-- Alarm & Wakelock -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Batterie-Optimierung deaktivieren (damit der Wecker auch im Schlaf funktioniert!) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Vibration -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Do Not Disturb / Schulmodus -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BEA">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:label="@string/app_name"
            android:theme="@style/Theme.BEA">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="juna" android:host="nfc" />
            </intent-filter>
        </activity>

        <!-- Fullscreen Alarm Activity -->
        <activity
            android:name=".alarm.AlarmActivity"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.BEA"
            android:exported="false" />

        <!-- Activity: Sleep-Reminder Erinnerung -->
        <activity
            android:name=".alarm.SleepReminderActivity"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.BEA"
            android:exported="false" />

        <!-- BroadcastReceiver: Alarm auslösen -->
        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver: Alarm stoppen -->
        <receiver
            android:name=".alarm.AlarmDismissReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver: Live-Notification neu anzeigen wenn weggewischt -->
        <receiver
            android:name=".worker.NotificationDismissReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver: Sleep-Reminder Wecker -->
        <receiver
            android:name=".alarm.BedtimeAlarmReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver: Sleep-Reminder dimissed -->
        <receiver
            android:name=".alarm.SleepReminderDismissReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver: Nach Neustart Alarm neu setzen -->
        <!-- QUICKBOOT_POWERON = Xiaomi/HTC, LOCKED_BOOT_COMPLETED = Android 7+ early boot -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Service: Alarm-Sound abspielen -->
        <service
            android:name=".alarm.AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Home Screen Widget -->
        <receiver
            android:name=".widget.NextLessonWidgetReceiver"
            android:label="Nächste Stunde"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/next_lesson_widget_info" />
        </receiver>

        <!-- Countdown Widget -->
        <receiver
            android:name=".widget.LessonCountdownWidgetReceiver"
            android:label="Wie lange noch?"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lesson_countdown_widget_info" />
        </receiver>

        <!-- List Widget -->
        <receiver
            android:name=".widget.TimetableListWidgetReceiver"
            android:label="Heutiger Plan"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/timetable_list_widget_info" />
        </receiver>

        <!-- Alarm Status Widget -->
        <receiver
            android:name=".widget.AlarmStatusWidgetReceiver"
            android:label="Wecker Status"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/alarm_status_widget_info" />
        </receiver>

        <!-- Widget Configuration Activity -->
        <activity
            android:name=".widget.WidgetConfigurationActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

    </application>

</manifest>